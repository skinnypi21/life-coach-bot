import ComingSoon from '@/components/ComingSoon';

export default function TrendsPage() {
  return (
    <ComingSoon
      emoji="📈"
      title="Trends"
      phase="Phase 4"
      note="Scores, satisfaction, and completion over time — including the pillar heatmap — arrive here."
    />
  );
}
